<%- include('../../partials/header.ejs') %>
<!-- header dipindahkan ke header.ejs lalu di include di index.ejs -->

<!-- Page Wrapper -->
<div id="wrapper">
  <%- include('../../partials/sidebar.ejs') %>
  <!-- sidebar dipindahkan ke sidebar.ejs lalu di include di index.ejs -->

  <!-- Content Wrapper -->
  <div id="content-wrapper" class="d-flex flex-column">
    <!-- Main Content -->
    <div id="content">
      <!-- navbar dipindahkan ke navbar.ejs lalu di include di index.ejs -->
      <%- include('../../partials/navbar.ejs') %>

      <!-- Begin Page Content -->
      <div class="container-fluid">
        <!-- <div class="alert alert-success" role="alert">A simple success alert—check it out!</div> -->
        <!-- <% if(alert.message != "") {%>
        <div class="alert alert-<%= alert.status %>" role="alert"><%= alert.message %></div>
        <%} %> -->
        <!-- pindahkan meesage ke partials ->message.ejs -->
        <%- include('../../partials/message.ejs') %>

        <!-- Page Heading -->
        <h1 class="h3 mb-4 text-gray-800">Category</h1>
        <!-- End page heading -->

        <!-- DataTales Example -->
        <%- include('table_category.ejs') %>
        <!-- End Datatales example -->
      </div>
      <!-- /.container-fluid -->
    </div>
    <!-- End of Main Content -->

    <!-- footer dipindahkan ke footer.ejs lalu di include di index.ejs -->
    <%- include('../../partials/footer.ejs') %>
  </div>
  <!-- End of Content Wrapper -->
</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
      <div class="modal-footer">
        <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
        <a class="btn btn-primary" href="login.html">Logout</a>
      </div>
    </div>
  </div>
</div>

<%- include('../../partials/js.ejs') %> <%- include('add_modal.ejs') %> <%- include('edit_modal.ejs') %>

<script>
  $("#dataTable").on("click", ".button-update", function () {
    let id = $(this).data("id");
    let name = $(this).data("name");
    // console.log(id + name)
    $("#edit-modal").modal("show");
    $(".id").val(id);
    $(".name").val(name);
  });
</script>
